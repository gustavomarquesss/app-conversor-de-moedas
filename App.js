import { StatusBar, SafeAreaView } from 'react-native';
import FormularioConversaoMoedas from './src/screens/FormularioConversaoMoedas/index';

export default function App() {
  return (
    <SafeAreaView>
      <FormularioConversaoMoedas/>
    </SafeAreaView>
  );
}
